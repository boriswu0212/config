.DS_Store
.secrets
.claude/

# Claude Code local settings (personal/team, not committed)
claude/settings.local.json
claude/hooks/

# Sensitive files
*.pem
*.key
*.p12
id_rsa*
id_ed25519*
homelab
homelab.pub
.env
.env.*

# SSH private keys backup
ssh/backup/

# SSH machine-local overrides (created by ssh/work-profile.sh)
ssh/config.local
